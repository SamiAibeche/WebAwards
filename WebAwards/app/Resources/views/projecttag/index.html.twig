{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProjectTag list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projectTag in projectTags %}
            <tr>
                <td><a href="{{ path('projecttag_show', { 'id': projectTag.id }) }}">{{ projectTag.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('projecttag_show', { 'id': projectTag.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('projecttag_edit', { 'id': projectTag.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('projecttag_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
